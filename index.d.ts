export = randomRgba;
/**
 * Generate a string in the CSS format `rgba(r, g, b, a)` with random values.
 * @param {number} alpha - optional opacity value: defaults to a random percentage
 */
declare function randomRgba(alpha: number): string;
